/** Google Timeline, iOS export: a top-level array using `geo:` strings. Bible §13. Session 2. */

import type { ParseResult } from '../types';

export function parseGoogleIos(_json: unknown): ParseResult {
	throw new Error('Not implemented until session 2');
}
