/** Google Timeline, Android export: `{ semanticSegments: [...] }`. Bible §13. Session 2. */

import type { ParseResult } from '../types';

export function parseGoogleAndroid(_json: unknown): ParseResult {
	throw new Error('Not implemented until session 2');
}
